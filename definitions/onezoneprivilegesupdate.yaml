type: object
description: Onezone privileges update request.
allOf:
  - $ref: '#/definitions/PrivilegesUpdateOperation'
  - $ref: '#/definitions/OnezonePrivileges'
example:
  operation: update
  privileges:
    - oz_groups_add_members
    - oz_groups_list
