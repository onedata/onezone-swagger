type: object
description: The list of group privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - group_change_data
        - group_create_space
        - group_invite_group
        - group_invite_user
        - group_join_group
        - group_join_space
        - group_leave_space
        - group_remove
        - group_remove_group
        - group_remove_user
        - group_set_privileges
        - group_view_data
example:
  privileges:
    - group_change_data
    - group_create_space
    - group_invite_group
    - group_invite_user
    - group_join_group
    - group_join_space
    - group_leave_space
    - group_remove
    - group_remove_group
    - group_remove_user
    - group_set_privileges
    - group_view_data
