type: object
description: The list of group privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - group_change_data
        - group_invite_user
        - group_remove_user
        - group_join_space
        - group_create_space
        - group_set_privileges
        - group_remove
        - group_leave_space
        - group_view_data
        - group_create_space_token
        - group_join_group
        - group_invite_group
        - group_remove_group
example:
  privileges:
    - group_change_data
    - group_invite_user
    - group_remove_user
    - group_join_space
    - group_create_space
    - group_set_privileges
    - group_remove
    - group_leave_space
    - group_view_data
    - group_create_space_token
    - group_join_group
    - group_invite_group
    - group_remove_group  
