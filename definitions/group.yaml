type: object
description: Group details.
properties:
  name:
    type: string
    description: The name of the space.
  type:
    type: string
    description: The type of the group.
    enum:
      - organization
      - unit
      - team
      - role
required:
  - name
  - type
example:
  name: Test group
  type: role