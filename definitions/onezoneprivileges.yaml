type: object
description: The list of Onezone privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - view_privileges
        - set_privileges
        - list_spaces
        - list_providers
        - list_providers_of_space
example:
  privileges:
    - view_privileges
    - set_privileges
    - list_spaces
    - list_providers
    - list_providers_of_space