type: array
description: A list of privileges in the handle service.
items:
  type: string
  enum:
    - handle_service_view
    - handle_service_register_handle
example: [handle_service_view, handle_service_register_handle]
