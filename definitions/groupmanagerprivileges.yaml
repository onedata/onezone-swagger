type: array
description: A list of manager privileges in the group.
items:
  type: string
  enum:
    - group_view
    - group_view_privileges

    - group_add_parent
    - group_leave_parent

    - group_add_child
    - group_remove_child

    - group_add_user
    - group_remove_user

    - group_add_harvester
    - group_remove_harvester
example: [group_view,group_view_privileges,group_add_parent,group_leave_parent,group_add_child,group_remove_child,
          group_add_user,group_remove_user,group_add_harvester,group_remove_harvester ]