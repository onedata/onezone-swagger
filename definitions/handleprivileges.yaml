type: object
description: The list of handle privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - view_handle
        - modify_handle
        - delete_handle
example:
  privileges:
    - view_handle
    - modify_handle
    - delete_handle
