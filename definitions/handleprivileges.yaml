type: object
description: The list of handle privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - unregister_handle
        - handle_change_data
        - handle_view_data
example:
  privileges:
    - unregister_handle
    - handle_change_data
    - handle_view_data