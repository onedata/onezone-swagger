type: object
description: The list of handle privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - delete_handle
        - modify_handle
        - view_handle
example:
  privileges:
    - delete_handle
    - modify_handle
    - view_handle
