type: object
description: The list of handle privileges.
properties:
  privileges:
    type: array
    items:
      type: string
      enum:
        - handle_delete
        - handle_update
        - handle_view
required:
  - privileges
example:
  operation:
    - grant
  privileges:
    - handle_delete
    - handle_update
    - handle_view
