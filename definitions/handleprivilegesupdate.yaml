type: object
description: Handle privileges update request.
properties:
  grant:
    $ref: '#/definitions/HandlePrivileges'
  revoke:
    $ref: '#/definitions/HandlePrivileges'

example:
  grant:
    - handle_view
    - handle_update
  revoke:
    - handle_delete
