get:
  operationId: list_handle_users
  tags:
    - Handle
  x-onedata-gri:
    type: od_handle
    id: ?BINDING(id)
    aspect: users
  x-onedata-permissions:
    - handle_view
    - oz_handles_list_relationships
  summary: List handle users
  x-bash-codegen-description: |
    Returns all users with access to a handle instance

    This operation requires `handle_view` privilege
    or `oz_handles_list_relationships` admin privilege.
  description: |
    Returns all users with access to a handle instance

    This operation requires `handle_view` privilege
    or `oz_handles_list_relationships` admin privilege.

    ***Example cURL requests***

    **Get handle users**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/handles/SADHLKJhlkASHDLAKSHDLKJHJjLH/users

    {
      "users": [
        "LAKSHDLKLKJhlkASHDJHJjL",
        "HDLKJHJjLLKLAKSJhlkASHD"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of identifiers of users with access to specific handle.
      schema:
        $ref: '#/definitions/Users'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Handle Id.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listHandleUsers id=DLAKSHDLKJHJjSADHLKJhlkASHLH"
