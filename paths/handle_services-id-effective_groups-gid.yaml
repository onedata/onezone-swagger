get:
  operationId: get_effective_handle_service_group
  tags:
    - Handle Service
  x-onedata-gri:
    type: od_group
    id: ?BINDING(gid)
    aspect: instance
    scope: shared
  x-onedata-auth-hint: ?THROUGH_HANDLE_SERVICE(?BINDING(id))
  x-onedata-permissions:
    - handle_service_view
  summary: Get effective handle service group
  x-bash-codegen-description: |
    Get details of a group with effective access to handle service.

    This operation requires `handle_service_view` privilege.
  description: |
    Get details of a group with effective access to handle service.

    This operation requires `handle_service_view` privilege.

    ***Example cURL requests***

    **Get effective handle service group details**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/handle_services/SADHLKJhlkASHDLAKSHDLKJHJjLH/effective_groups/HwUpk8jrwxKOe45uzLFX2GVa8lKEasj4q253sptVqF8

    {
      "groupId":"HwUpk8jrwxKOe45uzLFX2GVa8lKEasj4q253sptVqF8",
      "name":"Group name",
      "type":"team"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Group details returned.
      schema:
        $ref: '#/definitions/Group'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: gid
      in: path
      description: The ID of the group to return information about.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getEffectiveHandleServiceGroup id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 gid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk"
