get:
  operationId: get_effective_child_group
  tags:
    - Group
  x-onedata-gri:
    type: od_group
    id: ?BINDING(cid)
    aspect: instance
    scope: shared
  x-onedata-auth-hint: ?THROUGH_GROUP(?BINDING(id))
  x-onedata-permissions:
    - group_view
    - oz_group_list_groups
  summary: Get effective child group details
  x-bash-codegen-description: |
    Returns information about a specific effective child group.

    This operation requires `group_view` privilege.
    For administrator who does not belong to this group
    'oz_groups_list_groups' privilege is required.
  description: |
    Returns information about a specific effective child group.

    This operation requires `group_view` privilege.
    For administrator who does not belong to this group
    'oz_groups_list_groups' privilege is required.

    ***Example cURL requests***

    **Get effective child details**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/groups/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/effective_children/T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM

    {
      "groupId": "T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM",
      "name": "new_group1",
      "type": "role"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Information about a specific effective child group.
      schema:
        $ref: '#/definitions/Group'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: cid
    in: path
    description: Effective child group ID.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getEffectiveChildGroup id=HwUpk8jrwxKOe45uzLFX2GVC1lKEasj4q253sptVqF8 cid=AKLSKDHASDKASDGAHSGDHJASDHJJAHGDSJKHG"
