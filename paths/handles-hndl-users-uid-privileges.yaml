get:
  operationId: list_handle_user_privileges
  tags:
    - Handle
  x-onedata-gri:
    type: od_handle
    id: ?BINDING(hndl)
    aspect: "{user_privileges, ?BINDING(uid)}"
  x-onedata-permissions:
    - handle_view
    - oz_handles_view_privileges
  summary: List handle user privileges
  x-bash-codegen-description: |
    Returns user privileges for accessing a handle instance.

    This operation requires `handle_view` privilege
    or `oz_handles_view_privileges` administrator privilege.
  description: |
    Returns user privileges for accessing a handle instance.

    This operation requires `handle_view` privilege
    or `oz_handles_view_privileges` administrator privilege.

    ***Example cURL requests***

    **Get handle user privileges**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/handles/SADHLKJhlkASHDLAKSHDLKJHJjLH/users/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges

    {
      "privileges": [
        "handle_update",
        "handle_view"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of privileges of a user to a specific handle.
      schema:
        $ref: '#/definitions/HandlePrivileges'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: hndl
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: uid
      in: path
      description: The ID of the user of a handle service.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listHandleUserPrivileges hndl=DLAKSHDLKJHJjSADHLKJhlkASHLH uid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk"


patch:
  operationId: set_handle_user_privileges
  tags:
    - Handle
  x-onedata-gri:
    type: od_handle
    id: ?BINDING(hndl)
    aspect: "{user_privileges, ?BINDING(uid)}"
  x-onedata-permissions:
    - handle_update
    - oz_handles_set_privileges
  summary: Set handle user privileges
  x-bash-codegen-description: |
    Sets user privileges for accessing a handle instance.

    This operation requires `handle_update` privilege
    or `oz_handles_set_privileges` administrator privilege.
  description: |
    Sets user privileges for accessing a handle instance.

    This operation requires `handle_update` privilege
    or `oz_handles_set_privileges` administrator privilege.

    ***Example cURL requests***

    **Set handle user privileges**
    ```bash
    curl -u username:password -X PATCH -H "Content-type: application/json" \
    -d '{"privileges": ["handle_update"]}' \
    https://$HOST/api/v3/handles/SADHLKJhlkASHDLAKSHDLKJHJjLH/users/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: User privileges updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: hndl
      in: path
      description: The ID of the handle.
      required: true
      type: string
    - name: uid
      in: path
      description: The ID of the user of a handle.
      required: true
      type: string
    - name: privileges
      in: body
      description: The list of user privileges to a specific handle.
      required: true
      schema:
        $ref: '#/definitions/HandlePrivilegesUpdate'
  x-code-samples:
  - lang: Shell
    source: "echo '{\"privileges\": [\"handle_update\"]}' | onezone-rest-cli setHandleUserPrivileges hndl=DLAKSHDLKJHJjSADHLKJhlkASHLH uid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk -"
