get:
  operationId: get_user_cluster
  tags:
    - User
  x-onedata-gri:
    type: od_cluster
    id: ?BINDING(cid)
    aspect: instance
    scope: protected
  x-onedata-auth-hint: ?THROUGH_USER(?CLIENT_ID)
  summary: Get user's cluster details
  x-bash-codegen-description: |
    Returns the details of a specific user's cluster.

    This operation can be invoked on behalf of the current user only.
  description: |
    Returns the details of a specific user's cluster.

    This operation can be invoked on behalf of the current user only.

    ***Example cURL requests***

    **Get user's cluster details**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/user/clusters/Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY

    {
      "clusterId" : "Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY",
      "type" : "oneprovider",
      "serviceId" : "hxfMCWmquqAIjG1GeG2eZY8qvs8iRIn09HzjC",
      "name" : "My Provider",
      "domain" : "my-provider.example.com",
      "version" : "18.02.1",
      "build" : "cd624fd211-18",
      "online" : true,
      "proxy" : false
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Details of a specific cluster.
      schema:
        $ref: '#/definitions/Cluster'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: cid
    in: path
    description: Cluster Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getUserCluster cid=oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY"


delete:
  operationId: user_leave_cluster
  tags:
    - User
  x-onedata-gri:
    type: od_user
    id: ?CLIENT_ID
    aspect: "{cluster, ?BINDING(cid)}"
  summary: Leave cluster
  x-bash-codegen-description: |
    Removes the users membership in a specific cluster.

    This operation can be invoked on behalf of the current user only.
  description: |
    Removes the users membership in a specific cluster.

    This operation can be invoked on behalf of the current user only.

    ***Example cURL requests***

    **User leave cluster**
    ```bash
    curl -u username:password -X DELETE \
    https://$HOST/api/v3/onezone/user/clusters/oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY
    ```
  produces:
    - application/json
  responses:
    '204':
      description: The user is no longer a member of the cluster.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: cid
    in: path
    description: Cluster Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli userLeaveCluster cid=oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY"
