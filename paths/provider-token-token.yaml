get:
  operationId: get_provider_spaces_token
  x-onedata-service: onezone
  x-onedata-module: provider
  x-onedata-resource: token
  x-onedata-method: token_logic:get_issuer
  description: TODO
  produces:
    - application/json
  responses:
    '200':
      description: TODO
      schema:
        $ref : '#/definitions/spaceinvitetoken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: token
    in: path
    description: Token ID.
    type: string
    required: true

