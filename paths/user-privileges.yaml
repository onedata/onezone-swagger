get:
  operationId: list_current_user_privileges
  tags:
    - User
  x-onedata-gri:
    type: od_user
    id: ?CLIENT_ID
    aspect: oz_privileges
  x-onedata-permissions:
    - oz_view_privileges
  summary: List current user privileges
  x-bash-codegen-description: |
    Returns the list of currently authenticated user privileges for Onezone.

    This operation can be invoked on behalf of current user only and requires
    `oz_view_privileges` administrator privilege.
  description: |
    Returns the list of currently authenticated user privileges for Onezone.

    This operation can be invoked on behalf of current user only and requires
    `oz_view_privileges` administrator privilege.

    ***Example cURL requests***

    **Get users privileges to Onezone service**
    ```bash
    curl -u username:password -X GET  \
    https://$HOST:8443/api/v3/onezone/user/privileges

    {
      "privileges": []
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of user privileges in Onezone.
      schema:
        $ref: '#/definitions/OnezonePrivileges'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listCurrentUserPrivileges"


patch:
  operationId: set_current_user_privileges
  tags:
    - User
  x-onedata-gri:
    type: od_user
    id: ?CLIENT_ID
    aspect: oz_privileges
  x-onedata-permissions:
    - oz_set_privileges
  summary: Set current user privileges
  x-bash-codegen-description: |
    Modifies privileges for a user for Onezone.

    This operation can be invoked on behalf of current user only and requires
    `oz_set_privileges` administrator privilege.
  description: |
    Modifies privileges for a user for Onezone.

    This operation can be invoked on behalf of current user only and requires
    `oz_set_privileges` administrator privilege.

    ***Example cURL requests***

    **Modifies users privileges to Onezone service**
    ```bash
    curl -u username:password  -H "Content-type: application/json"  -X PATCH \
    -d '{"privileges":["oz_spaces_list"]}' \
    https://$HOST:8443/api/v3/onezone/user/privileges
    ```
  responses:
    '204':
      description: The user privileges have been updated.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: data
    in: body
    description: User Onezone privileges.
    schema:
      $ref: '#/definitions/OnezonePrivilegesUpdate'
  x-code-samples:
  - lang: Shell
    source: "echo '[\"oz_spaces_list\", \"oz_spaces_list\"]' | onezone-rest-cli setCurrentUserPrivileges -"


delete:
  operationId: remove_current_user_privileges
  tags:
    - User
  x-onedata-gri:
    type: od_user
    id: ?CLIENT_ID
    aspect: oz_privileges
  x-onedata-permissions:
    - oz_set_privileges
  summary: Remove current user privileges
  x-bash-codegen-description: |
    Removes all privileges for a user to Onezone.

    This operation can be invoked on behalf of current user only and requires
    `oz_set_privileges` administrator privilege.
  description: |
    Removes all privileges for a user to Onezone.

    This operation can be invoked on behalf of current user only and requires
    `oz_set_privileges` administrator privilege.

    ***Example cURL requests***

    **Delete all user privileges to Onezone service**
    ```bash
    curl -u username:password  -X DELETE  \
    https://$HOST:8443/api/v3/onezone/users/privileges
    ```
  responses:
    '202':
      description: The user privileges will be cleared.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli removeCurrentUserPrivileges"
