get:
  operationId: get_privileges
  tags:
    - Zone
  x-onedata-gri:
    type: oz_worker
    aspect: privileges
  x-onedata-permissions: []
  summary: Get all admin privileges.
  x-bash-codegen-description: |
    Returns list of all possible Onezone admin privileges.

    This endpoint does not require authentication.
  description: |
    Returns list of all possible Onezone admin privileges.

    This endpoint does not require authentication.

    ***Example cURL requests***

    **Get list of all Onezone admin privileges**
    ```bash
     curl https://$HOST/api/v3/onezone/privileges
    ```
  produces:
    - application/json
  responses:
    '200':
      description: List of all Onezone admin privileges
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getPrivileges"
