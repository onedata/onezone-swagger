post:
  operationId: provider_space_support
  x-onedata-service: onezone
  x-onedata-module: provider
  x-onedata-resource: ssupport
  x-onedata-method: space_logic:support
  summary: Adds support for specific space within provider.
  description: Adds support for specific space within provider.
  produces:
    - application/json
  responses:
    '201':
      description: Space has been supported successfully - the space ID is returned in the response entity - 201 /provider/spaces/{id}
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: pid
    in: body
    schema:
      $ref: '#/definitions/spacesupporttoken'