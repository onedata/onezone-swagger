get:
  operationId: get_space_marketplace_data
  tags:
    - Space
  x-onedata-gri:
    type: od_space
    id: ?BINDING(id)
    aspect: marketplace_data
    scope: protected
  summary: Get space marketplace details
  x-bash-codegen-description: |
    Returns the marketplace relaited details about a specific space.

    This operation requires can be performed by any user but only for spaces 
    advertised in marketplace.
  description: |
    Returns the marketplace relaited details about a specific space.

    This operation requires can be performed by any user but only for spaces 
    advertised in marketplace.

    ***Example cURL requests***

    **Get space marketplace details**
    ```bash
    curl -u admin:password -X GET \
    https://$ZONE_HOST/api/v3/onezone/space_marketplace/$SPACE_ID

    {
      "name": "My Private space",
      "description": "My Private data",
      "organizationName": "",
      "tags": ["images", "sport"],
      "providerNames": ["krakow", "paris"],
      "totalSupportSize": 1000000,
      "creationTime": 1576152793
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Marketplace related information about a space.
      schema:
        $ref: '#/definitions/SpaceMarketplaceData'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Space Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getSpaceMarketplaceData id=b752ceafabb662b4e5728b2ded25cdd1"
