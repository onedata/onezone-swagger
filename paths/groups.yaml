post:
  operationId: create_group
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: groups
  x-onedata-method: group_logic:create
  summary: Creates a new group.
  description: Creates a new group.
  consumes:
    - application/json
  responses:
    '201':
      description: ID of the group created in the form /groups/{id} is returned in the response entity.
      schema:
        type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API unavailable at this URL.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: data
    in: body
    description: Group properties.
    schema:
      $ref: '#/definitions/group'
    required: true
