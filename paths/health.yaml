get:
  operationId: health
  tags:
    - Zone
  x-onedata-gri:
    type: oz_worker
    aspect: health
  x-onedata-permissions: []
  summary: Perform healthckeck
  x-bash-codegen-description: |
    Returns status code indicating oneprovider service health status.
        This endpoint does not require authentication.
  description: >
    Returns status code indicating oneprovider service health status.
        This endpoint does not require authentication.
  produces:
    - application/json
  responses:
    '200':
      description: Service is healthy.
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
