post:
  operationId: create_provider_registration_token
  tags:
    - Provider
  x-onedata-gri:
    type: od_provider
    aspect: provider_registration_token
  x-onedata-permissions:
    - oz_providers_invite
  summary: Create provider registration token
  x-bash-codegen-description: |
    Creates a token allowing new provider to register in Onezone.

    This operation requires `oz_providers_invite` privilege.
  description: |
    Creates a token allowing new provider to register in Onezone.

    This operation requires `oz_providers_invite` privilege.

    ***Example cURL requests***

    **Create space user invite token**
    ```bash
    curl -u username:password -X POST \
    https://$HOST:8443/api/v3/onezone/providers/token

    {
      "token": "MDAxNmxvY2F00aW9uIHJlZ2lzdHJ5CjAwM2JpZGVudGlmaWVyIG00zcEJ2UDVuOHhkQUNhdk9hbTlyNnIwNldPSzVhc"
    }
    ```
  produces:
    - application/json
  consumes:
    - application/json
  responses:
    '200':
      description: The token for registering in Onezone.
      schema:
        $ref: '#/definitions/SpaceInviteToken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli createProviderRegistrationToken"
