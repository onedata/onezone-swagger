get:
  operationId: get_group_privileges
  tags:
    - Group
  x-onedata-gri:
    type: od_group
    aspect: privileges
  x-onedata-permissions: []
  summary: Get all group privileges.
  x-bash-codegen-description: |
    Returns list of all possible group privileges.

    This endpoint does not require authentication.
  description: |
    Returns list of all possible group privileges.

    This endpoint does not require authentication.

    ***Example cURL requests***

    **Get list of all group privileges**
    ```bash
     curl https://$HOST/api/v3/onezone/group/privileges
    ```
  produces:
    - application/json
  responses:
    '200':
      description: List of all group privileges
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getGroupPrivileges"
