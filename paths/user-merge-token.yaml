get:
  operationId: get_account_merge_token
  tags:
    - User
  x-onedata-service: onezone
  x-onedata-module: token
  x-onedata-method: token_logic:create
  summary: Creates a new account merge token
  x-bash-codegen-description: |
    Creates a new account merge token.

    This operation can be invoked on behalf of current user only.
  description: |
    Creates a new account merge token.

    This operation can be invoked on behalf of current user only.
  produces:
    - application/json
  responses:
    '200':
      description: Returns the new account merge token.
      schema:
        $ref: '#/definitions/AccountMergeToken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'  
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getAccountMergeToken"
