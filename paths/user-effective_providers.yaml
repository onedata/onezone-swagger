get:
  operationId: get_user_effective_providers
  tags:
    - User
  x-onedata-service: onezone
  x-onedata-module: user
  x-onedata-method: user_logic:get_effective_providers
  summary: Get user effective providers
  x-bash-codegen-description: |
    Returns the list of user's effective providers.

    This operation can be invoked on behalf of current user only.
  description: |
    Returns the list of user's effective providers.

    This operation can be invoked on behalf of current user only.

    ***Example cURL requests***

    **Get user effective providers**
    ```bash
    curl -k -u admin:password -X GET \
    https://$HOST:8443/api/v3/onezone/user/effective_providers

    {
      "providers": ["LKJASHGDFKLJHASKLJDH"]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of providers.
      schema:
        type: object
        properties:
          providers:
            type: array
            items:
              type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getUserEffectiveProviders"
