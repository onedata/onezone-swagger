get:
  operationId: get_space_privileges
  tags:
    - Space
  x-onedata-gri:
    type: od_space
    aspect: privileges
  x-onedata-permissions: []
  summary: Get all space privileges.
  x-bash-codegen-description: |
    Returns list of all possible space privileges.

    This endpoint does not require authentication.
  description: |
    Returns list of all possible space privileges.

    This endpoint does not require authentication.

    ***Example cURL requests***

    **Get list of all space privileges**
    ```bash
     curl https://$HOST/api/v3/onezone/space/privileges
    ```
  produces:
    - application/json
  responses:
    '200':
      description: List of all space privileges
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getSpacePrivileges"
