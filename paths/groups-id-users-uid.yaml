get:
  operationId: get_group_user
  tags:
    - Group
  x-onedata-gri:
    type: od_user
    id: ?BINDING(uid)
    aspect: instance
    scope: shared
  x-onedata-auth-hint: ?THROUGH_GROUP(?BINDING(id))
  x-onedata-permissions:
    - group_view,
    - oz_users_view
  summary: Get group user details
  x-bash-codegen-description: |
    Returns the public information about a user {uid} in group {id}.

    This operation requires `group_view` privilege.
    For administrator who does not belong to this group
    `oz_users_view` privilege is required.
  description: |
    Returns the public information about a user {uid} in group {id}.

    This operation requires `group_view` privilege.
    For administrator who does not belong to this group
    `oz_users_view` privilege is required.

    ***Example cURL requests***

    **Generate user group invite token**
    ```bash
    curl -u admin:password -X GET \
    https://$HOST/api/v3/onezone/groups/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/users/lb0NvUXIVguzjQ3dBOXAyd1c61fWKB5dKJDQ6YvB7a0

    {
      "userId": "lb0NvUXIVguzjQ3dBOXAyd1c61fWKB5dKJDQ6YvB7a0",
      "alias": "admin",
      "name": "admin"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Information about a group user.
      schema:
        $ref: '#/definitions/User'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group Id.
    type: string
    required: true
  - name: uid
    in: path
    description: User Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getGroupUser id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 uid=lb0NvUXIVguzjQ3dBOXAyd1c61fWKB5dKJDQ6YvB7a0"


put:
  operationId: add_group_user
  tags:
    - Group
  x-onedata-gri:
    type: od_group
    id: ?BINDING(id)
    aspect: "{user, ?BINDING(uid)}"
  x-onedata-permissions:
    - oz_groups_add_relationships
    - oz_users_add_relationships
  summary: Add user to group
  x-bash-codegen-description: |
    Returns the public information about a user {uid} in group {id}.

    Optionally, user privileges in this group can be specified
    in the request body.

    This operation requires `oz_groups_add_relationships` and `oz_users_add_relationships` admin privileges.
  description: |
    Returns the public information about a user {uid} in group {id}.

    Optionally, user privileges in this group can be specified
    in the request body.

    This operation requires `oz_groups_add_relationships` admin privilege.

    ***Example cURL requests***

    **Generate user group invite token**
    ```bash
    curl -u admin:password -X PUT \
    https://$HOST/api/v3/onezone/groups/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/users/lb0NvUXIVguzjQ3dBOXAyd1c61fWKB5dKJDQ6YvB7a0
    ```
  produces:
    - application/json
  responses:
    '201':
      description: >
        URI of the added user in form https://$HOST/api/onezone/v3/groups/{id}/users/{uid} is
        returned in the response `Location` header.
      headers:
        Location:
          description: URI of the added user.
          type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group Id.
    type: string
    required: true
  - name: uid
    in: path
    description: User Id.
    type: string
    required: true
  - name: privileges
    in: body
    required: false
    description: The list of privileges for the
    schema:
      $ref: '#/definitions/GroupPrivileges'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli addGroupUser id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 uid=lb0NvUXIVguzjQ3dBOXAyd1c61fWKB5dKJDQ6YvB7a0"


delete:
  operationId: remove_group_user
  tags:
    - Group
  x-onedata-gri:
    type: od_group
    id: ?BINDING(id)
    aspect: "{user, ?BINDING(uid)}"
  x-onedata-permissions:
    - group_remove_user
    - oz_groups_remove_relationships
    - oz_users_remove_relationships
  summary: Remove user from group
  x-bash-codegen-description: |
    Removes the user {uid} from a group {id} (the user will no longer have
    access to spaces accessible to the group).

    This operation requires `group_remove_user` privilege.
    For administrator who does not belong to this group
    `oz_groups_remove_relationships` and `oz_users_remove_relationships` privileges are required.
  description: |
    Removes the user {uid} from a group {id} (the user will no longer have access
    to spaces accessible to the group).

    This operation requires `group_remove_user` privilege.
    For administrator who does not belong to this group
    `oz_groups_remove_relationships` and `oz_users_remove_relationships` privileges are required.

    ***Example cURL requests***

    **Remove user from group**
    ```bash
    curl -u username:password -X DELETE \
    https://$HOST/api/v3/onezone/groups/Qi4QT2oalnfQu0SzTK1HgXxKuUrlRppb68rr_UhwC50/users/ivMnRLb2WYNApEmP-j3SF0NsqBgdHG7iel89FHY802w
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: The user was removed from the group.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group Id.
    type: string
    required: true
  - name: uid
    in: path
    description: User Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli removeGroupUser id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 uid=lb0NvUXIVguzjQ3dBOXAyd1c61fWKB5dKJDQ6YvB7a0"
