get:
  operationId: get_supported_spaces
  x-onedata-service: onezone
  x-onedata-module: provider
  x-onedata-resource: spaces
  x-onedata-method: provider_logic:get_spaces
  summary: Returns the list of spaces managed by the Oneprovider that performed the request.
  description: Returns the list of spaces managed by the Oneprovider that performed the request.
  produces:
    - application/json
  responses:
    '200':
      description: The list of space ID's.
      schema:
        type: array
        items:
          type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'


post:
  operationId: create_provider_space
  x-onedata-service: onezone
  x-onedata-module: provider
  x-onedata-resource: spaces
  x-onedata-method: space_logic:create
  summary:  Creates a new space on behalf of the Oneprovider that performed the request.
  description: Creates a new space on behalf of the Oneprovider that performed the request.
  consumes:
    - application/json
  responses:
    '201':
      description: The ID of the created space returned in the response entity, i.e. 201 /provider/spaces/{id}
      schema:
        type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API unavailable at this URL.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: data
    in: body
    description: New space parameters.
    schema:
      $ref: '#/definitions/space'
    required: true

