get:
  operationId: list_handle_service_group_privileges
  tags:
    - Handle Service
  x-onedata-gri:
    type: od_handle_service
    id: ?BINDING(id)
    aspect: "{group_privileges, ?BINDING(gid)}"
  x-onedata-permissions:
    - handle_service_view
  summary: List handle service group privileges
  x-bash-codegen-description: |
    Returns group privileges for accessing a handle service instance.

    This operation requires `handle_service_view` privilege.
  description: |
    Returns group privileges for accessing a handle service instance.

    This operation requires `handle_service_view` privilege.

    ***Example cURL requests***

    **Get handle service group privileges**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/handle_services/SADHLKJhlkASHDLAKSHDLKJHJjLH/groups/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges

    {
      "privileges": [
        "handle_service_delete",
        "handle_service_view"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of privileges of a group to a specific handle service.
      schema:
        $ref: '#/definitions/HandleServicePrivileges'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: gid
      in: path
      description: The ID of the group of a handle service.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listHandleServiceGroupPrivileges id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 gid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk"


patch:
  operationId: set_handle_service_group_privileges
  tags:
    - Handle Service
  x-onedata-gri:
    type: od_handle_service
    id: ?BINDING(id)
    aspect: "{group_privileges, ?BINDING(gid)}"
  x-onedata-permissions:
    - handle_service_update
  summary: Set handle service groups privileges
  x-bash-codegen-description: |
    Sets group privileges for accessing a handle service instance.

    This operation requires `handle_service_update` privilege.
  description: |
    Sets group privileges for accessing a handle service instance.

    This operation requires `handle_service_update` privilege.

    ***Example cURL requests***

    **Set handle service group privileges**
    ```bash
    curl -u username:password -X PATCH  -H "Content-type: application/json" \
    -d '{"privileges": ["handle_service_register_handle"]}' \
    https://$HOST/api/v3/handle_services/SADHLKJhlkASHDLAKSHDLKJHJjLH/groups/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: Group privileges updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: gid
      in: path
      description: The ID of the group of a handle service.
      required: true
      type: string
    - name: privileges
      in: body
      description: The list of group privileges to specific handle service.
      required: true
      schema:
        $ref: '#/definitions/HandleServicePrivilegesUpdate'
  x-code-samples:
  - lang: Shell
    source: "echo '{\"privileges\": [\"handle_service_register_handle\"]}' | onezone-rest-cli setHandleServiceGroupPrivileges id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 gid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk -"
