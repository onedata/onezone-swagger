get:
  operationId: get_cluster
  tags:
    - Cluster
  x-onedata-gri:
    type: od_cluster
    id: ?BINDING(id)
    aspect: instance
    scope: protected
  x-onedata-permissions:
    - oz_clusters_view
  summary: Get cluster details
  x-bash-codegen-description: |
    Returns the details about a specific cluster.

    If called by user who is not member of the cluster, requires `oz_clusters_view`
    privilege.
  description: |
    Returns the details about a specific cluster.

    If called by user who is not member of the cluster, requires `oz_clusters_view`
    privilege.

    ***Example cURL requests***

    **Get cluster details**
    ```bash
    curl -u admin:password -X GET \
    https://$HOST/api/v3/onezone/clusters/803ZirkUfdiWDd4W3bI4QaPBog_0kCdUddUIsgAxi5I

    {
      "clusterId" : "Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY",
      "type" : "oneprovider",
      "serviceId" : "hxfMCWmquqAIjG1GeG2eZY8qvs8iRIn09HzjC",
      "workerVersion": {
        "release": "18.02.1",
        "build": "1-gfabf02d",
        "gui": "129c549fbe9b3e730c3d9910492228566e39e1236945766a74381405cc57fb04"
      },
      "onepanelVersion": {
        "release": "18.02.1",
        "build": "3-89fc6bad",
        "gui": "68e7e1472adb719ddf2d88908775091dfb10e5d0b916f06125ea1af4056044e3"
      },
      "online" : true,
      "onepanelProxy" : false
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Information about a cluster.
      schema:
        $ref: '#/definitions/Cluster'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Cluster Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getCluster id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3"


patch:
  operationId: modify_cluster
  tags:
    - Cluster
  x-onedata-gri:
    type: od_cluster
    id: ?BINDING(id)
    aspect: instance
  x-onedata-permissions:
    - cluster_update
    - oz_clusters_update
  summary: Modify cluster details
  x-bash-codegen-description: |
    Updates the details about a cluster.

    This operation requires `cluster_update` privilege
    or `oz_clusters_update` admin privilege.
  description: |
    Updates the details about a cluster.

    This operation requires `cluster_update` privilege
    or `oz_clusters_update` admin privilege.

    ***Example cURL requests***

    **Change cluster name**
    ```bash
    curl -u username:password -H "Content-type: application/json" \
    -X PATCH -d '{"name": "new_cluster12"}' \
    https://$HOST/api/v3/onezone/clusters/56IZ6lRxcbz4OEbrr7vPI52UA7E6WwkqQ6bJCtW5PLE
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: Cluster has been updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Cluster Id.
    type: string
    required: true
  - name: data
    in: body
    description: Cluster data.
    schema:
      $ref: '#/definitions/ClusterUpdateRequest'
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli modifyCluster id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3 proxy=true"
