get:
  operationId: get_group_effective_cluster
  tags:
    - Group
  x-onedata-gri:
    type: od_cluster
    id: ?BINDING(cid)
    aspect: instance
    scope: protected
  x-onedata-auth-hint: ?THROUGH_GROUP(?BINDING(id))
  summary: Get group's effective cluster details
  x-bash-codegen-description: |
    Returns information about a specific effective cluster to which the group belongs.

    This operation can be invoked on behalf of the current group only.
  description: |
    Returns information about a specific effective cluster to which the group belongs.

    This operation can be invoked on behalf of the current group only.

    ***Example cURL requests***

    **Get group's effective cluster details**
    ```bash
    curl -u groupname:password -X GET \
    https://$HOST/api/v3/onezone/group/clusters/Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY

    {
      "clusterId" : "Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY",
      "type" : "oneprovider",
      "serviceId" : "hxfMCWmquqAIjG1GeG2eZY8qvs8iRIn09HzjC",
      "name" : "My Provider",
      "domain" : "my-provider.example.com",
      "version" : "18.02.1",
      "build" : "cd624fd211-18",
      "online" : true,
      "proxy" : false
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Returns the details of a specific group's effective cluster.
      schema:
        $ref: '#/definitions/Cluster'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group Id.
    type: string
    required: true
  - name: cid
    in: path
    description: Cluster Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getGroupEffectiveCluster cid=oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY"
