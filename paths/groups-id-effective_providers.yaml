get:
  operationId: get_effective_group_providers
  tags:
    - Group
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: providers
  x-onedata-method: group_logic:get_effective_group_providers
  x-onedata-permissions:
    - group_view_data
  summary: Get effective group's providers
  x-bash-codegen-description: |
    Returns the effective list of providers supporting group spaces.

    This operation requires `group_view_data` privilege.
  description: |
    Returns the effective list of providers supporting group spaces.

    This operation requires `group_view_data` privilege.

    ***Example cURL requests***

    **Get group effective providers**
    ```bash
    curl -u username:password -X GET \
    https://$HOST:8443/api/v3/onezone/groups/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/effective_providers
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The effective list of providers supporting group spaces.
      schema:
        $ref: '#/definitions/Providers'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getEffectiveGroupProviders id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8"
