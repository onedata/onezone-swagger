get:
  operationId: get_user_effective_spaces
  tags:
    - User
  x-onedata-service: onezone
  x-onedata-module: user
  x-onedata-method: user_logic:get_effective_spaces
  summary: Get user effective spaces
  x-bash-codegen-description: |
    Returns the list of user's effective spaces.

    This operation can be invoked on behalf of current user only.
  description: |
    Returns the list of user's effective spaces.

    This operation can be invoked on behalf of current user only.

    ***Example cURL requests***

    **Get user effective spaces**
    ```bash
    curl -k -u admin:password -X GET \
    https://$HOST:8443/api/v3/onezone/user/effective_spaces

    {
      "spaces": ["admins"]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of spaces.
      schema:
        type: object
        properties:
          spaces:
            type: array
            items:
              type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getUserEffectiveSpaces"
