get:
  operationId: get_nested_group_token
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: ninvite
  x-onedata-method: token_logic:create
  summary: Creates a token allowing new user to join a nested group.
  description: Creates a token allowing new user to join a nested group.
  produces:
    - application/json
  responses:
    '200':
      description: The token for joining a specific group.
      schema:
        $ref : '#/definitions/groupinvitetoken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
