get:
  operationId: get_group_user_privileges
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: upriv
  x-onedata-method: group_logic:get_privileges
  summary: Returns the list of user privileges in a specific group.
  description: Returns the list of user privileges in a specific group.
  produces:
    - application/json
  responses:
    '200':
      description: The list of user privileges in a group.
      schema:
        type: array
        items:
          type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: uid
    in: path
    description: User ID.
    type: string
    required: true

put:
  operationId: set_user_group_privileges
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: upriv
  x-onedata-method: group_logic:set_privileges
  summary: Adds specific privileges for a user in a group.
  description: Adds specific privileges for a user in a group.
  responses:
    '200':
      description: The user privileges have been added.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: uid
    in: path
    description: User ID.
    type: string
    required: true
  - name: data
    in: body
    description: User privileges.
    schema:
      type: array
      items:
        type: string
    required: true

