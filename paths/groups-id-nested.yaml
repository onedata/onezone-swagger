get:
  operationId: get_nested_groups
  tags:
    - Group
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: nested_groups
  x-onedata-method: group_logic:get_nested_groups
  x-onedata-permissions:
    - group_view_data
  summary: Get subgroups
  x-bash-codegen-description: |
    Returns the list of subgroups of a specific group.

    This operation requires `group_view_data` privilege.
  description: |
    Returns the list of subgroups of a specific group.

    This operation requires `group_view_data` privilege.

    ***Example cURL requests***

    **Get subgroups**
    ```bash
    curl -k -u admin:password -X GET \
    https://$HOST:8443/api/v3/onezone/groups/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/nested

    {
      "nested_groups": []
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of subgroup ID's.
      schema:
        type: array
        items:
          type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'  
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getNestedGroups id=HwUpk8jrwxKOe45uzLFX2GVC1lKEasj4q253sptVqF8"
