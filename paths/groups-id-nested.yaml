get:
  operationId: get_nested_groups
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: nested_groups
  x-onedata-method: group_logic:get_nested_groups
  summary: Returns the list of nested groups.
  description: Returns the list of nested groups.
  produces:
    - application/json
  responses:
    '200':
      description: The list of nested group ID's.
      schema:
        type: array
        items:
          type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
