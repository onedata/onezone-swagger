get:
  operationId: get_named_token_of_provider
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{provider_named_token, ?BINDING(id)}"
  x-onedata-permissions:
    - oz_tokens_manage
  summary: Get named token of a provider
  x-bash-codegen-description: |
    Returns the information about a specific provider's named token.

    Requires `oz_tokens_manage` admin privilege.
  description: |
    Returns the information about a specific provider's named token.

    Requires `oz_tokens_manage` admin privilege.

    ***Example cURL requests***

    **Get named token of a provider**
    ```bash
    curl -H "x-auth-token: $TOKEN" -X GET \
    https://$HOST/api/v3/onezone/providers/6ebe7ac282e0188b5336b5d8cfa564d5/tokens/named/my-token-1
    {
      "name": "my-token-1",
      "id": "2b5d0dd5aa6443a69277b5ce0544fec2",
      "subject": {
        "type": "provider",
        "id": "6ebe7ac282e0188b5336b5d8cfa564d5"
      },
      "type": {
        "inviteToken": {
          "subtype": "userJoinCluster",
          "clusterId": "fb73f7ceff5abd995357abbe01c812ce"
        }
      },
      "caveats": [
        {
          "type": "time",
          "validUntil": 1571147494
        },
        {
          "type": "ip",
          "whitelist": [
            "189.34.15.0/8",
            "127.0.0.0/24",
            "167.73.12.17"
          ]
        }
      ],
      "metadata": {
        "creationTime": 1564721024,
        "usageLimit": 15,
        "usageCount": 3,
        "privileges": [
          "space_view",
          "space_read_data",
          "space_view_views",
          "space_view_statistics"
        ],
        "custom": {
          "jobName": "experiment-15",
          "vm": "worker156.cloud.local"
        }
      },
      "revoked": false,
      "token": "MDAxNWxvY2F0aW9uIG9uZXpvbmUKMDAzYmlkZW50aWZpZXIgOEhmSEFSSGdrbHFCa1pWSTR"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Returns the details of a specific named token.
      schema:
        $ref: '#/definitions/NamedToken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Provider Id
      type: string
      required: true
    - name: name
      in: path
      description: Token name (unique for provider)
      type: string
      required: true
  x-code-samples:
    - lang: Shell
      source: "onezone-rest-cli getNamedTokenOfProvider id=6ebe7ac282e0188b5336b5d8cfa564d5 name=my-token-1"


put:
  operationId: create_named_token_for_provider
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{provider_named_token, ?BINDING(id)}"
  x-onedata-permissions:
    - oz_tokens_manage
  summary: Create named token for a provider
  x-bash-codegen-description: |
    Creates a new named token for specific provider. The token name must be unique
    for the provider.

    Requires `oz_tokens_manage` admin privilege.
  description: |
    Creates a new named token for specific provider. The token name must be unique
    for the provider.

    Requires `oz_tokens_manage` admin privilege.

    ***Example cURL requests***

    **Create named token for a provider**
    ```bash
    curl -H "x-auth-token: $TOKEN" -X PUT -d '{"type": {"accessToken":{}}}' \
    -H 'Content-type: application/json' \
    https://$HOST/api/v3/onezone/providers/6ebe7ac282e0188b5336b5d8cfa564d5/tokens/named/new-token
    ```
  consumes:
    - application/json
  responses:
    '200':
      description: Returns the newly created named token in serialized form.
      schema:
        type: object
        properties:
          token:
            $ref: '#/definitions/SerializedToken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Provider Id
      type: string
      required: true
    - name: name
      in: path
      description: Token name (unique for provider)
      type: string
      required: true
    - name: data
      in: body
      description: Properties of the new named token.
      schema:
        $ref: '#/definitions/NamedTokenCreateRequest'
      required: true
  x-code-samples:
    - lang: Shell
      source: "onezone-rest-cli createNamedTokenForProvider id=6ebe7ac282e0188b5336b5d8cfa564d5 name=new-token"


patch:
  operationId: modify_named_token_of_provider
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{provider_named_token, ?BINDING(id)}"
  x-onedata-permissions:
    - oz_tokens_manage
  summary: Modify named token of a provider
  x-bash-codegen-description: |
    Modifies a specific provider's named token. Supports toggling the revoked flag
    and modifying the metadata.

    Requires `oz_tokens_manage` admin privilege.
  description: |
    Modifies a specific provider's named token. Supports toggling the revoked flag
    and modifying the metadata.

    Requires `oz_tokens_manage` admin privilege.

    ***Example cURL requests***

    **Modify named token of a provider**
    ```bash
    curl -H "x-auth-token: $TOKEN" -H "Content-type: application/json" \
    -X PATCH -d '{"revoked": true}' \
    https://$HOST/api/v3/onezone/providers/6ebe7ac282e0188b5336b5d8cfa564d5/tokens/named/my-token-1
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: Provider's named token has been updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Provider Id
      type: string
      required: true
    - name: name
      in: path
      description: Token name (unique for provider)
      type: string
      required: true
    - name: data
      in: body
      description: Named token update request
      schema:
        $ref: '#/definitions/NamedTokenUpdateRequest'
      required: true
  x-code-samples:
    - lang: Shell
      source: "onezone-rest-cli modifyNamedTokenOfProvider id=6ebe7ac282e0188b5336b5d8cfa564d5 name=my-token-1 revoked==true"


delete:
  operationId: delete_named_token_of_provider
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{provider_named_token, ?BINDING(id)}"
  x-onedata-permissions:
    - oz_tokens_manage
  summary: Delete named token of a provider
  x-bash-codegen-description: |
    Deletes a specific provider's named token.

    Requires `oz_tokens_manage` admin privilege.
  description: |
    Deletes a specific provider's named token.

    Requires `oz_tokens_manage` admin privilege.

    ***Example cURL requests***

    **Delete named token of a provider**
    ```bash
    curl -H "x-auth-token: $TOKEN" -X DELETE \
    https://$HOST/api/v3/onezone/providers/6ebe7ac282e0188b5336b5d8cfa564d5/tokens/named/my-token-1
    ```
  produces:
    - application/json
  responses:
    '204':
      description: The named token was deleted.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Provider Id
      type: string
      required: true
    - name: name
      in: path
      description: Token name (unique for provider)
      type: string
      required: true
  x-code-samples:
    - lang: Shell
      source: "onezone-rest-cli deleteNamedTokenOfProvider id=6ebe7ac282e0188b5336b5d8cfa564d5 name=my-token-1"
