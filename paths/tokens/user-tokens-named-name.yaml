get:
  operationId: get_named_token_of_current_user
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{user_named_token, ?CLIENT_ID}"
  summary: Get named token of current user
  x-bash-codegen-description: |
    Returns the information about a user's named token.

    This operation can be invoked on behalf of the current user only.
  description: |
    Returns the information about a user's named token.

    This operation can be invoked on behalf of the current user only.

    ***Example cURL requests***

    **Get named token of current user**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/user/tokens/named/my-token-1
    {
      "name": "my-token-1",
      "id": "2b5d0dd5aa6443a69277b5ce0544fec2",
      "subject": {
        "type": "user",
        "id": "c26bab23d12f7389c3c311caf9c15902"
      },
      "type": {
        "inviteToken": {
          "subtype": "userJoinCluster",
          "clusterId": "fb73f7ceff5abd995357abbe01c812ce"
        }
      },
      "caveats": [
        {
          "type": "time",
          "validUntil": 1571147494
        },
        {
          "type": "ip",
          "whitelist": [
            "189.34.15.0/8",
            "127.0.0.0/24",
            "167.73.12.17"
          ]
        }
      ],
      "metadata": {
        "creationTime": 1564721024,
        "usageLimit": 15,
        "usageCount": 3,
        "privileges": [
          "space_view",
          "space_read_data",
          "space_view_views",
          "space_view_statistics"
        ],
        "custom": {
          "jobName": "experiment-15",
          "vm": "worker156.cloud.local"
        }
      },
      "revoked": false,
      "token": "MDAxNWxvY2F0aW9uIG9uZXpvbmUKMDAzYmlkZW50aWZpZXIgOEhmSEFSSGdrbHFCa1pWSTR"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Returns the details of a specific named token.
      schema:
        $ref: '#/definitions/NamedToken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: name
    in: path
    description: Token name (unique for user)
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getNamedTokenOfCurrentUser name=my-token-1"


put:
  operationId: create_named_token_for_current_user
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{user_named_token, ?CLIENT_ID}"
  summary: Create named token for current user
  x-bash-codegen-description: |
    Creates a new named token for the user. The token name must be unique
    for the user.

    In case of invite tokens, invite / add member privileges are required in
    the target entity to create a token. For example, `space_add_group` in
    the space when creating a `groupJoinSpace` invite token.

    This operation can be invoked on behalf of the current user only.
  description: |
    Creates a new named token for the user. The token name must be unique
    for the user.

    In case of invite tokens, invite / add member privileges are required in
    the target entity to create a token. For example, `space_add_group` in
    the space when creating a `groupJoinSpace` invite token.

    This operation can be invoked on behalf of the current user only.

    ***Example cURL requests***

    **Create named token for current user**
    ```bash
    curl -u username:password -X PUT -d '{"type": {"accessToken":{}}}' \
    -H 'Content-type: application/json' \
    https://$HOST/api/v3/onezone/user/tokens/named/new-token
    ```
  consumes:
    - application/json
  responses:
    '200':
      description: Returns the newly created named token in serialized form.
      schema:
        type: object
        properties:
          token:
            $ref: '#/definitions/SerializedToken'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: name
    in: path
    description: Token name (unique for user)
    type: string
    required: true
  - name: data
    in: body
    description: Properties of the new named token.
    schema:
      $ref: '#/definitions/NamedTokenCreateRequest'
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli createNamedTokenForCurrentUser name=new-token"


patch:
  operationId: modify_named_token_of_current_user
  tags:
  - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{user_named_token, ?CLIENT_ID}"
  summary: Modify named token of current user
  x-bash-codegen-description: |
    Modifies a user's named token. Supports toggling the revoked flag and
    modifying the metadata.

    This operation can be invoked on behalf of the current user only.
  description: |
    Modifies a user's named token. Supports toggling the revoked flag and
    modifying the metadata.

    This operation can be invoked on behalf of the current user only.

    ***Example cURL requests***

    **Modify named token of current user**
    ```bash
    curl -u username:password -H "Content-type: application/json" \
    -X PATCH -d '{"revoked": true}' \
    https://$HOST/api/v3/onezone/user/tokens/named/my-token-1
    ```
  consumes:
  - application/json
  responses:
    '204':
      description: User's named token has been updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: name
    in: path
    description: Token name (unique for user)
    type: string
    required: true
  - name: data
    in: body
    description: Named token update request
    schema:
      $ref: '#/definitions/NamedTokenUpdateRequest'
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli modifyNamedTokenOfCurrentUser name=my-token-1 revoked==true"


delete:
  operationId: delete_named_token_of_current_user
  tags:
    - Token
  x-onedata-gri:
    type: od_token
    id: ?BINDING(name)
    aspect: "{user_named_token, ?CLIENT_ID}"
  summary: Delete named token of current user
  x-bash-codegen-description: |
    Deletes a user's named token.

    This operation can be invoked on behalf of the current user only.
  description: |
    Deletes a user's named token.

    This operation can be invoked on behalf of the current user only.

    ***Example cURL requests***

    **Delete named token of current user**
    ```bash
    curl -u username:password -X DELETE \
    https://$HOST/api/v3/onezone/user/tokens/named/my-token-1
    ```
  produces:
    - application/json
  responses:
    '204':
      description: The named token was deleted.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: name
    in: path
    description: Token name (unique for user)
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli deleteNamedTokenOfCurrentUser name=my-token-1"
