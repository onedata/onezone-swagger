post:
  operationId: group_spaces_join
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: sjoin
  x-onedata-method: space_logic:join
  description: Joins the group to an existing space based on provide space invitation token.
  consumes:
    - application/json
  responses:
    '200':
      description: User has successfully joined a group.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: data
    in: body
    description: Space join token.
    schema:
      $ref: '#/definitions/spaceinvitetoken'