get:
  operationId: list_harvester_groups
  tags:
    - Harvester
  x-onedata-gri:
    type: od_harvester
    id: ?BINDING(id)
    aspect: groups
  summary: List harvester groups
  x-onedata-permissions:
    - harvester_view
    - oz_harvesters_list_relationships
  x-bash-codegen-description: |
    Returns the list of groups belonging to a specific harvester.

    If called by user who is not member of the harvester, requires `harvester_view`
    privilege or `oz_harvesters_list_relationships` admin privilege.
  description: |
    Returns the list of groups belonging to a specific harvester.

    If called by user who is not member of the harvester, requires `harvester_view`
    privilege or `oz_harvesters_list_relationships` admin privilege.

    ***Example cURL requests***

    **Get harvester groups**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/harvesters/56IZ6lRxcbz4OEbrr7vPI52UA7E6WwkqQ6bJCtW5PLE/groups

    {
      "groups": [
        "T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM",
        "e3piG9yg9877lagR7aHayk73te9gAMXxQvjBycwvnaow"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of all group Ids that belong to a specific harvester
      schema:
        $ref: '#/definitions/Groups'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Harvester Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listHarvesterGroups id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3"


post:
  operationId: create_harvester_group
  tags:
  - Harvester
  x-onedata-gri:
    type: od_harvester
    id: ?BINDING(id)
    aspect: "group"
  x-onedata-auth-hint: ?AS_USER(?CLIENT_ID)
  x-onedata-permissions:
    - harvester_add_group
    - oz_groups_create
    - oz_harvesters_add_relationships
  summary: Create group in harvester
  x-bash-codegen-description: |
    Creates a new group belonging to the harvester of {id}.

    This operation requires `harvester_add_group` privilege.
    For administrator who does not belong to this group
    `oz_harvesters_add_relationships` and `oz_groups_create`
    privileges are required.
  description: |
    Creates a new group belonging to the harvester of {id}.

    This operation requires `harvester_add_group` privilege.
    For administrator who does not belong to this group
    `oz_harvesters_add_relationships` and `oz_groups_create`
    privileges are required.

    ***Example cURL requests***

    **Create group in harvester**
    ```bash
    curl -u username:password -X POST \
    https://$HOST/api/v3/onezone/harvesters/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/groups
    ```
  produces:
  - application/json
  responses:
    '201':
      description: >
        URI of the added group in form https://$HOST/api/onezone/v3/harvesters/{id}/groups/{gid} is
        returned in the response `Location` header.
      headers:
        Location:
          description: URI of the created group.
          type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Harvester Id.
    type: string
    required: true
  - name: data
    in: body
    description: Group properties.
    schema:
      $ref: '#/definitions/GroupCreateRequest'
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli createHarvesterGroup id=HwUpk8jrwxKOe45uzLFX2GVC1lKEasj4q253sptVqF8 name==Group2 type==team"
