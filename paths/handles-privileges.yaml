get:
  operationId: get_handle_privileges
  tags:
    - Handle
  x-onedata-gri:
    type: od_handle
    aspect: privileges
  x-onedata-permissions: []
  summary: Get all handle privileges.
  x-bash-codegen-description: |
    Returns list of all possible handle privileges.

    This endpoint does not require authentication.
  description: |
    Returns list of all possible handle privileges.

    This endpoint does not require authentication.

    ***Example cURL requests***

    **Get list of all handle privileges**
    ```bash
     curl https://$HOST/api/v3/onezone/handle/privileges
    ```
  produces:
    - application/json
  responses:
    '200':
      description: List of all handle privileges
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getHandlePrivileges"
