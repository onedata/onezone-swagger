get:
  operationId: get_group_handle_service
  tags:
    - Group
  x-onedata-gri:
    type: od_handle_service
    id: ?BINDING(hsid)
    aspect: instance
    scope: protected
  x-onedata-auth-hint: ?THROUGH_GROUP(?BINDING(id))
  x-onedata-permissions:
    - group_view
    - oz_handle_services_view
  summary: Get group handle service details
  x-bash-codegen-description: |
    Returns the details of a specific handle service.

    This operation requires `group_view` privilege.
    For administrator who does not belong to this group
    `oz_handle_services_view` privilege is required.
  description: |
    Returns the details of a specific handle service.

    This operation requires `group_view` privilege.
    For administrator who does not belong to this group
    `oz_handle_services_view` privilege is required.

    ***Example cURL requests***

    **Get handle service details**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/groups/ALASDLKJALKSDJKLASD/handle_services/oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY

    {
        "name": "MyCommunity Handle service",
        "handleServiceId": "SADHLKJhlkASHDLAKSHDLKJHJjLH",
        "proxyEndpoint": "https://localhost:17000/handle_proxy",
        "serviceProperties": {
            "allowTemplateOverride": false,
            "doiEndpoint": "/doi",
            "host": "https://mds.test.datacite.org",
            "identifierTemplate": "{{space.name}}-{{space.guid}}",
            "mediaEndpoint": "/media",
            "metadataEndpoint": "/metadata",
            "password": "pa$$word",
            "prefix": 10.5072,
            "type": "DOI",
            "username": "alice"
        }
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Returns the details of a specific handle service.
      schema:
        $ref: '#/definitions/HandleService'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: hsid
    in: path
    description: Handle service ID.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getGroupHandleService id=ALASDLKJALKSDJKLASD hsid=oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY"


delete:
  operationId: remove_group_handle_service
  tags:
    - Group
  x-onedata-gri:
    type: od_group
    id: ?BINDING(id)
    aspect: "{handle_service, ?BINDING(hsid)}"
  summary: Remove group handle service
  x-onedata-permissions:
    - group_leave_handle_service
    - oz_groups_remove_relationships
    - oz_handle_services_remove_relationships
  x-bash-codegen-description: |
    Removes the group ownership or membership in a specific handle service.

    This operation requires `group_leave_handle_service` privilege.
    For administrator who does not belong to this group
    `oz_groups_remove_relationships` and `oz_handle_services_remove_relationships` privilege is required.
  description: |
    Removes the group ownership or membership in a specific handle service.

    This operation requires `group_leave_handle_service` privilege.
    For administrator who does not belong to this group
    `oz_groups_remove_relationships` and `oz_handle_services_remove_relationships` privilege is required.

    ***Example cURL requests***

    **Delete user handle service**
    ```bash
    curl -u username:password -X DELETE \
    https://$HOST/api/v3/onezone/groups/AJKLASHJKDHKAJSHD/handle_services/oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY
    ```
  produces:
    - application/json
  responses:
    '204':
      description: The handle service will be deleted.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: hsid
    in: path
    description: Handle service Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli removeGroupHandleService hsid=oOVF-KrO1P6rpA0LFgNVI8NxuhxyQMUnrYzjAnKiyAY"
