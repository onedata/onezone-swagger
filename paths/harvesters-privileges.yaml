get:
  operationId: get_harvester_privileges
  tags:
    - Harvester
  x-onedata-gri:
    type: od_harvester
    aspect: privileges
  x-onedata-permissions: []
  summary: Get all harvester privileges.
  x-bash-codegen-description: |
    Returns list of all possible harvester privileges.

    This endpoint does not require authentication.
  description: |
    Returns list of all possible harvester privileges.

    This endpoint does not require authentication.

    ***Example cURL requests***

    **Get list of all harvester privileges**
    ```bash
     curl https://$HOST/api/v3/onezone/harvester/privileges
    ```
  produces:
    - application/json
  responses:
    '200':
      description: List of all harvester privileges
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getHarvesterPrivileges"
