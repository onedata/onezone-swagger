post:
  operationId: authenticate_user
  x-onedata-service: onezone
  x-onedata-module: user
  x-onedata-method: auth_logic:authenticate_user
  summary: Authenticates user using authentication token.
  description: Authenticates user using authentication token through Onezone web interface.
  consumes:
    - application/json
  responses:
    '200':
      description: User authenticated successfully.
      schema:
        type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API unavailable at this URL.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: data
    in: body
    description: User authentication token.
    schema:
      $ref: '#/definitions/authenticationtoken'
    required: true