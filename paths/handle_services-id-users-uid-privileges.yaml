get:
  operationId: get_handle_service_user_privileges
  tags:
    - Handle Service
  x-onedata-gri:
    type: od_handle_service
    id: ?BINDING(id)
    aspect: "{user_privileges, ?BINDING(uid)}"
  x-onedata-permissions:
    - handle_service_view
  summary: List handle service user privileges
  x-bash-codegen-description: |
    Returns user privileges for accessing a handle service instance.

    This operation requires `handle_service_view` privilege.
  description: |
    Returns user privileges for accessing a handle service instance.

    This operation requires `handle_service_view` privilege.

    ***Example cURL requests***

    **Get handle service user privileges**
    ```bash
    curl -u username:password -X GET \
    https://$HOST:8443/api/v3/handle_services/SADHLKJhlkASHDLAKSHDLKJHJjLH/users/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges

    {
      "privileges": [
        "handle_service_delete",
        "handle_service_view"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of privileges of a user to a specific handle service.
      schema:
        $ref: '#/definitions/HandleServicePrivileges'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: uid
      in: path
      description: The ID of the user of a handle service.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listHandleServiceUserPrivileges id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 uid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk"


patch:
  operationId: set_handle_service_user_privileges
  tags:
    - Handle Service
  x-onedata-gri:
    type: od_handle_service
    id: ?BINDING(id)
    aspect: "{user_privileges, ?BINDING(uid)}"
  x-onedata-permissions:
    - handle_service_update
  summary: Set handle service user privileges
  x-bash-codegen-description: |
    Sets user privileges for accessing a handle service instance.

    This operation requires `handle_service_update` privilege.
  description: |
    Sets user privileges for accessing a handle service instance.

    This operation requires `handle_service_update` privilege.

    ***Example cURL requests***

    **Set handle service user privileges**
    ```bash
    curl -u username:password -X PATCH  -H "Content-type: application/json" \
    -d '{"privileges": ["handle_service_register_handle"]}' \
    https://$HOST:8443/api/v3/handle_services/SADHLKJhlkASHDLAKSHDLKJHJjLH/users/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: User privileges updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: uid
      in: path
      description: The ID of the user of a handle service.
      required: true
      type: string
    - name: privileges
      in: body
      description: The list of user privileges to specific handle service.
      required: true
      schema:
        $ref: '#/definitions/HandleServicePrivileges'
  x-code-samples:
  - lang: Shell
    source: "echo '{\"privileges\": [\"handle_service_register_handle\"]}' | onezone-rest-cli setHandleServiceUserPrivileges id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 uid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk -"
