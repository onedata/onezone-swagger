get:
  operationId: get_effective_provider_user
  tags:
    - Provider
  x-onedata-gri:
    type: od_user
    id: ?BINDING(uid)
    aspect: instance
    scope: protected
  x-onedata-auth-hint: ?THROUGH_PROVIDER(?BINDING(pid))
  x-onedata-permissions:
    - oz_providers_list_users
  summary: Get effective user of provider
  x-bash-codegen-description: |
    Returns the details of an effective user of a specific provider.

    This operation requires `oz_providers_list_users` administrator privilege.
  description: |
    Returns the details of an effective user of a specific provider.

    This operation requires `oz_providers_list_users` administrator privilege.

    ***Example cURL requests***

    **Get effective user of a provider**
    ```bash
    curl -u username:password -X GET  \
    https://$HOST:8443/api/v3/onezone/providers/ASDJKHASDASD5465asdvASDasd/effective_users/njOzyvXybAYvki10HXRCeJd_IlLHS4XEklgghmJjDpo

    {
       "name" : "John Doe",
       "login" : "username",
       "connectedAccounts" : [],
       "userId" : "njOzyvXybAYvki10HXRCeJd_IlLHS4XEklgghmJjDpo",
       "emailList" : []
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The effective user details.
      schema:
        $ref: '#/definitions/User'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: pid
      description: Provider ID.
      type: string
      in: path
      required: true
    - name: uid
      description: User ID.
      type: string
      in: path
      required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getEffectiveProviderUser pid=ASDJKHASDASD5465asdvASDasd uid=KJHAGSDJKHGASJKHDGAKJHSDGKHJASD"
