get:
  operationId: get_cluster_privileges
  tags:
    - Cluster
  x-onedata-gri:
    type: od_cluster
    aspect: privileges
  x-onedata-permissions: []
  summary: Get all cluster privileges.
  x-bash-codegen-description: |
    Returns list of all possible cluster privileges.

    This endpoint does not require authentication.
  description: |
    Returns list of all possible cluster privileges.

    This endpoint does not require authentication.

    ***Example cURL requests***

    **Get list of all cluster privileges**
    ```bash
     curl https://$HOST/api/v3/onezone/cluster/privileges
    ```
  produces:
    - application/json
  responses:
    '200':
      description: List of all cluster privileges
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getClusterPrivileges"
