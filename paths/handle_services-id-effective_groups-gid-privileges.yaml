get:
  operationId: get_effective_handle_service_group_privileges
  tags:
    - Handle Service
  x-onedata-gri:
    type: od_handle_service
    id: ?BINDING(id)
    aspect: "{eff_group_privileges, ?BINDING(gid)}"
  x-onedata-permissions:
    - handle_service_view
    - oz_handle_services_view_privileges
  summary: Get effective handle service group privileges
  x-bash-codegen-description: |
    Returns effective group privileges for accessing a handle service instance.

    This operation requires `handle_service_view` privilege
    or `oz_handle_services_view_privileges` administrator privilege.
  description: |
    Returns effective group privileges for accessing a handle service instance.

    This operation requires `handle_service_view` privilege
    or `oz_handle_services_view_privileges` administrator privilege.

    ***Example cURL requests***

    **Get handle service group privileges**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/handle_services/SADHLKJhlkASHDLAKSHDLKJHJjLH/effective_groups/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges

    {
      "privileges": [
        "handle_service_delete",
        "handle_service_view"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of privileges of a group to a specific handle service.
      schema:
        $ref: '#/definitions/HandleServicePrivileges'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: The ID of the handle service.
      required: true
      type: string
    - name: gid
      in: path
      description: The ID of the group of a handle service.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getEffectiveHandleServiceGroupPrivileges id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 gid=hlkASHDLAKSHDLKJHJjLHSADHLKJhlk"
