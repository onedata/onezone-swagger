get:
  operationId: get_harvester_index
  tags:
    - Harvester
  x-onedata-gri:
    type: od_harvester
    id: ?BINDING(id)
    aspect: "{index, ?BINDING(iid)}"
    scope: private
  x-onedata-permissions:
    - harvester_view
    - oz_harvesters_view
  summary: Get harvester index details
  x-bash-codegen-description: |
    Returns details about a specific index in the harvester.

    For users who are members of harvester it requires `harvester_view`.
    For administrators who do not have to be members of this harvester,
    `oz_harvesters_view` privilege is required.
  description: |
    Returns details about a specific index in the harvester.

    For users who are members of harvester it requires `harvester_view`.
    For administrators who do not have to be members of this harvester,
    `oz_harvesters_view` privilege is required.

    ***Example cURL requests***

    **Get harvester space details**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/harvesters/IkHBv8CoAFm5FCswhAJynbFU4fj26yiE1lhpK3p-0Y8/indices/H8ez0CwDZ7JMYRWn1ipmBpgJHPXzIXj0-upGkf9tk

    {
      "indexId": "H8ez0CwDZ7JMYRWn1ipmBpgJHPXzIXj0-upGkf9tk",
      "name": "example"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Details about a specific index in a harvester.
      schema:
        $ref: '#/definitions/HarvesterIndex'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Harvester Id.
    type: string
    required: true
  - name: iid
    in: path
    description: Index Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getHarvesterIndex id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3 iid=T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM"

post:
  operationId: query_harvester_index
  tags:
    - Harvester
  x-onedata-gri:
    type: od_harvester
    id: ?BINDING(id)
    aspect: "{query, ?BINDING(iid)}"
  summary: Query harvester index.
  x-bash-codegen-description: |
    Performs query to index {iid} in harvester {id}.

    This operation does not require any privileges when the harvester is public 
    otherwise being member of the harvester is required.

    Request body depends on selected `harvester_plugin`. This is example for `elasticsearch_plugin`.
  description: |
    Performs query to index {iid} in harvester {id}.

    This operation does not require any privileges when the harvester is public 
    otherwise being member of the harvester is required.

    Request body depends on selected `harvester_plugin`. This is example for `elasticsearch_plugin`.
    
    ***Example cURL requests***

    **query harvester index**
    ```bash
    curl -u username:password  -H "Content-type: application/json" -X POST \
    -d '{ "method" : "get", "path" : ""}'
     https://$HOST/api/v3/onezone/harvesters/9ueUeoZA6KXxNgzlvqmmrbzqE_BQiaHEEDC21sY1Kuc/indices/qE_BQiaHEEDC21sY1Kuc9ueUeoZA6KXxNgzlvqmmrbz \
    ```
  consumes:
    - application/json
  responses:
    '200':
      description: Response of harvester backend for given request.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Harvester Id.
      type: string
      required: true
    - name: iid
      in: path
      description: Index Id.
      type: string
      required: true
    - name: query
      in: body
      schema:
        $ref: '#/definitions/HarvesterQuery'
      required: false
  x-code-samples:
    - lang: Shell
      source: "onezone-rest-cli queryHarvesterIndex id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3 iid=T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM"


patch:
  operationId: modify_harvester_index
  tags:
    - Harvester
  x-onedata-gri:
    type: od_harvester
    id: ?BINDING(id)
    aspect: "{index, ?BINDING(iid)}"
  x-onedata-permissions:
    - harvester_update
    - oz_harvesters_update
  summary: Modify harvester index
  x-bash-codegen-description: |
    Updates harvester index.

    This operation requires `harvester_update` privilege
    or `oz_harvesters_update` admin privilege.
  description: |
    Updates harvester index.

    This operation requires `harvester_update` privilege
    or `oz_harvesters_update` admin privilege.

    ***Example cURL requests***

    **Change harvester name**
    ```bash
    curl -u username:password -H "Content-type: application/json" \
    -X PATCH -d '{"config": {}}' \
    https://$HOST/api/v3/onezone/harvesters/56IZ6lRxcbz4OEbrr7vPI52UA7E6WwkqQ6bJCtW5PLE/indices/T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: Harvester index has been updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Harvester Id.
      type: string
      required: true
    - name: iid
      in: path
      description: Index Id.
      type: string
      required: true
    - name: data
      in: body
      description: The new name of the index.
      schema:
        type: object
        properties:
          name:
            type: string
            description: The new name of the index.
          guiPluginName:
            type: string
            description: The new gui plugin name of the index.
      required: true
  x-code-samples:
    - lang: Shell
      source: "onezone-rest-cli modifyHarvesterIndex id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3 name==\"New name\""


delete:
  operationId: remove_harvester_index
  tags:
    - Harvester
  x-onedata-gri:
    type: od_harvester
    id: ?BINDING(id)
    aspect: "{index, ?BINDING(iid)}"
  x-onedata-permissions:
    - harvester_update
    - oz_harvesters_update
  summary: Remove harvester index
  x-bash-codegen-description: |
    Removes index from a specific harvester.

    This operation requires `harvester_update` privilege
    or `oz_harvesters_update` admin privilege.
  description: |
    Removes index from a specific harvester.

    This operation requires `harvester_update` privilege
    or `oz_harvesters_update` admin privilege.

    ***Example cURL requests***

    **Remove harvester support**
    ```bash
    curl -u username:password -X DELETE \
    https://$HOST/api/v3/onezone/harvesters/IkHBv8CoAFm5FCswhAJynbFU4fj26yiE1lhpK3p-0Y8/indices/H8ez0CwDZ7JMYRWn1ipmBpgJHPXzIXj0-upGkf9tk
    ```
  responses:
    '204':
      description: This index have been removed from given harvester.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Harvester Id.
    type: string
    required: true
  - name: iid
    in: path
    description: Index Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli removeHarvesterIndex id=bI4QaPBog_0kCdUddUIsgAxi5I803ZirkUfdiWDd4W3 iid=T5x_HhFYOnILOCUf9OqgExw00RwaU2MXT5122oWk_sM"
