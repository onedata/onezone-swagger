get:
  operationId: oz_users_list
  tags:
    - User
  x-onedata-gri:
    type: od_user
    aspect: list
  x-onedata-permissions:
    - oz_users_list
  summary: List all users
  x-bash-codegen-description: |
    Returns the list of all users in the system.

    Requires `oz_users_list` administrator privilege.
  description: |
    Returns the list of all users in the system.

    Requires `oz_users_list` administrator privilege.

    ***Example cURL requests***

    **List all users in the system**
    ```bash
     curl -u username:password -X GET https://$HOST/api/v3/onezone/users
    ```
  produces:
    - application/json
  responses:
    '200':
      description: >
        The list of user ID's registered within the Onezone service.
      schema:
        $ref: '#/definitions/Users'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli ozUsersList"
