get:
  operationId: get_provider_group
  tags:
    - Provider
  x-onedata-service: onezone
  x-onedata-module: providers
  x-onedata-resource: users
  x-onedata-method: provider_logic:get_user
  summary: Get user of provider
  x-bash-codegen-description: |
    Returns the details of a user of a specific provider.

    This operation requires `list_users_of_provider` privilege.
  description: |
    Returns the details of a user of a specific provider.

    This operation requires `list_users_of_provider` privilege.

    ***Example cURL requests***

    **List user of a provider**
    ```bash
    curl -ksS -u username:password -X GET  \
    https://$HOST:8443/api/v3/onezone/providers/ASDJKHASDASD5465asdvASDasd/groups/KJHAGSDJKHGASJKHDGAKJHSDGKHJASD

    {
      "groupId":"KJHAGSDJKHGASJKHDGAKJHSDGKHJASD",
      "name":"new_group",
      "type":"role"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The group ID.
      schema:
        $ref: '#/definitions/Group'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'  
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: pid
      description: Provider ID.
      type: string
      in: path
      required: true
    - name: gid
      description: Group ID.
      type: string
      in: path
      required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getProviderGroup pid=ASDJKHASDASD5465asdvASDasd gid=KJHAGSDJKHGASJKHDGAKJHSDGKHJASD"
