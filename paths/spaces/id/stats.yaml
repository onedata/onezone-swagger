get:
  operationId: get_space_stats
  tags:
    - Space
  x-onedata-gri:
    type: od_space
    id: ?BINDING(id)
    aspect: stats
    scope: private
  x-onedata-permissions:
    - oz_spaces_view
  summary: Get space stats
  x-bash-codegen-description: |
    Returns space statistics, including synchronization progress and capacity
    usage (NYI) for providers and storages that support the space.

    This operation requires any of the following authentication:
    * as user who is an effective member of the space (`{id}`),
    * as provider that supports the space (`{id}`),
    * as user who has `oz_spaces_view` admin privilege.
  description: |
    Returns space statistics, including synchronization progress and capacity
    usage (NYI) for providers and storages that support the space.

    This operation requires any of the following authentication:
    * as user who is an effective member of the space (`{id}`),
    * as provider that supports the space (`{id}`),
    * as user who has `oz_spaces_view` admin privilege.

    ***Example cURL requests***

    **Get space stats**
    ```bash
    curl -u admin:password -X GET \
    https://$HOST/api/v3/onezone/spaces/$SPACE_ID/stats

    {
      "syncProgressPerProvider": {
        "296ebe3c20e9666dc489b647f8647f12": {
          "296ebe3c20e9666dc489b647f8647f12": {
            "seq": 1450,
            "timestamp": 1571147494
          },
          "dcf12429647c204896eebe3b6f686967": {
            "seq": 49870,
            "timestamp": 1563819329
          }
        },
        "dcf12429647c204896eebe3b6f686967": {
          "296ebe3c20e9666dc489b647f8647f12": {
            "seq": 1390,
            "timestamp": 1580525418
          },
          "dcf12429647c204896eebe3b6f686967": {
            "seq": 50312,
            "timestamp": 1575049210
          }
        }
      }
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Space statistics.
      schema:
        $ref: '#/definitions/SpaceStats'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Space Id.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getSpaceStats id=b752ce4fa6b662b4e5728b2ded25cdd1"
