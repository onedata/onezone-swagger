patch:
  operationId: update_support_parameters_of_provider
  tags:
    - Space
  x-onedata-gri:
    type: od_space
    id: ?BINDING(id)
    aspect: "{support_parameters, ?BINDING(pid)}"
  x-onedata-permissions:
    - space_update
    - cluster_update
    - oz_spaces_update
  summary: Update space support parameters of provider
  x-bash-codegen-description: |
    Updates space support parameters of a provider in the space.

    Authorization depends on the modified parameters (see respective descriptions).
  description: |
    Updates space support parameters of a provider in the space.

    Authorization depends on the modified parameters (see respective descriptions).

    ***Example cURL requests***

    **Update space support parameters of provider**
    ```bash
    curl -u username:password -H "Content-type: application/json" \
    -X PATCH -d '{"dirStatsServiceEnabled": true}' \
    https://$HOST/api/v3/onezone/spaces/$SPACE_ID/providers/$PROVIDER_ID/support_parameters
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: Space support parameters have been updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Space Id.
      type: string
      required: true
    - name: pid
      in: path
      description: Provider Id.
      type: string
      required: true
    - name: data
      in: body
      schema:
        $ref: '#/definitions/SupportParameters'
  x-code-samples:
    - lang: Shell
      source: "echo '{\"dirStatsServiceEnabled\": true}' | onezone-rest-cli updateSupportParametersOfProvider id=b752ceafabb662b4e5728b2ded25cdd1 pid=ea662b7a7bded25cdd1fb2b4eb2852c5"