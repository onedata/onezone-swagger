get:
  operationId: get_effective_group_handles
  tags:
    - Group
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-method: group_logic:get_effective_handles
  summary: Get effective group handles
  x-bash-codegen-description: |
    Returns the list of effective groups' handles.

  description: |
    Returns the list of effective groups' handles.

    ***Example cURL requests***

    **Get group handles**
    ```bash
    curl -u username:password -X GET \
    https://$HOST:8443/api/v3/onezone/groups/LKJAHGSDLKJHALSDkjashdasdk/effective_handles

    {
      "handles": ["IkHB2CCoAFm5FCswhAJynbFU4fj26yiE1lhpK3p-0Y8"]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: >
        Returns the list of group handles.
      schema:
        type: object
        properties:
          handles:
            description: The list of effective group handles ID's.
            type: array
            items:
              type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getEffectiveGroupHandles id=LKJAHGSDLKJHALSDkjashdasdk"
