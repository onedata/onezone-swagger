get:
  operationId: get_provider_space
  tags:
    - Provider
  x-onedata-gri:
    type: od_space
    id: ?BINDING(sid)
    aspect: instance
    scope: protected
  x-onedata-auth-hint: ?THROUGH_PROVIDER(?BINDING(id))
  x-onedata-permissions:
    - oz_spaces_view
  summary: Get space supported by provider
  x-bash-codegen-description: |
    Returns the details of space supported by a specific provider.

    This operation requires `oz_spaces_view` admin privilege.
  description: |
    Returns the details of space supported by a specific provider.

    This operation requires `oz_spaces_view` admin privilege.

    ***Example cURL requests***

    **Get space supported by provider**
    ```bash
    curl -u username:password -X GET  \
    https://$HOST/api/v3/onezone/providers/ASDJKHASDASD5465asdvASDasd/spaces/Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY

    {
       "providers" : {
          "hxfMCWmquqAIjG1GeG2eZY8qvs8iRIn09HzjCJilnSE" : 5368709120
       },
       "spaceId" : "Xnp1JVpWfL8_stHJgct76AFALjRsI0W3rNs1nfMwqnY",
       "name" : "Test"
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The space details.
      schema:
        $ref: '#/definitions/Space'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      description: Provider Id.
      type: string
      in: path
      required: true
    - name: sid
      description: Space Id.
      type: string
      in: path
      required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getProviderSpace id=ASDJKHASDASD5465asdvASDasd sid=KJHAGSDJKHGASJKHDGAKJHSDGKHJASD"
