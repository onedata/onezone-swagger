get:
  operationId: get_nested_group
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: nested_group
  x-onedata-method: group_logic:get_nested_group
  summary: Returns information about a specific nested group.
  description: Returns information about a specific nested group.
  produces:
    - application/json
  responses:
    '200':
      description: Information about a specific nested group.
      schema:
        $ref: '#/definitions/group'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: nid
    in: path
    description: Nested group ID.
    type: string
    required: true


delete:
  operationId: delete_nested_group
  x-onedata-service: onezone
  x-onedata-module: group
  x-onedata-resource: nested_group
  x-onedata-method: group_logic:remove_nested_group
  summary: Removes a specific subgroup with {nid} from parent group with {id}.
  description: Removes a specific subgroup with {nid} from parent group with {id}.
  responses:
    '200':
      description: A nested group has been removed successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/error'
    '404':
      description: Onezone REST API not available.
      schema:
        $ref: '#/definitions/error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: nid
    in: path
    description: Nested group ID.
    type: string
    required: true