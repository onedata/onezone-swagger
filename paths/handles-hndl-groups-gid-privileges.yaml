get:
  operationId: list_handle_group_privileges
  tags:
    - Handle
  x-onedata-gri:
    type: od_handle
    id: ?BINDING(hndl)
    aspect: "{group_privileges, ?BINDING(gid)}"
  x-onedata-permissions:
    - handle_view
  summary: List handle group privileges
  x-bash-codegen-description: |
    Returns group privileges for accessing a handle instance.

    This operation requires `handle_view` privilege.
  description: |
    Returns group privileges for accessing a handle instance.

    This operation requires `handle_view` privilege.

    ***Example cURL requests***

    **Get handle group privileges**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/handles/DLAKSHDLKJHJjSADHLKJhlkASHLH/groups/HJjLHSADHhlkASHDLAKSHDLKJLKJhlk/privileges

    {
      "privileges": [
        "handle_view"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of privileges of a group to a specific handle.
      schema:
        $ref: '#/definitions/HandlePrivileges'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: hndl
      in: path
      description: The ID of the handle.
      required: true
      type: string
    - name: gid
      in: path
      description: The ID of the group of a handle.
      required: true
      type: string
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli listHandleGroupPrivileges hndl=DLAKSHDLKJHJjSADHLKJhlkASHLH gid=HJjLHSADHhlkASHDLAKSHDLKJLKJhlk"


patch:
  operationId: set_handle_group_privileges
  tags:
    - Handle
  x-onedata-gri:
    type: od_handle
    id: ?BINDING(hndl)
    aspect: "{group_privileges, ?BINDING(gid)}"
  x-onedata-permissions:
    - handle_update
  summary: Set handle groups privileges
  x-bash-codegen-description: |
    Sets group privileges for accessing a handle instance.

    This operation requires `handle_update` privilege.
  description: |
    Sets group privileges for accessing a handle instance.

    This operation requires `handle_update` privilege.

    ***Example cURL requests***

    **Set handle group privileges**
    ```bash
    curl -u username:password -X PATCH -H "Content-type: application/json" \
    -d '{"privileges": ["handle_view_data"]}' \
    https://$HOST/api/v3/handles/SADHLKJhlkASHDLAKSHDLKJHJjLH/groups/hlkASHDLAKSHDLKJHJjLHSADHLKJhlk/privileges
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: Group privileges updated successfully.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: hndl
      in: path
      description: The Onedata ID of the handle.
      required: true
      type: string
    - name: gid
      in: path
      description: The ID of the group of a handle.
      required: true
      type: string
    - name: privileges
      in: body
      description: The list of group privileges to a specific handle.
      required: true
      schema:
        $ref: '#/definitions/HandlePrivilegesUpdate'
  x-code-samples:
  - lang: Shell
    source: "echo '[{\"privileges\": [\"handle_view_data\"]}' | onezone-rest-cli setHandleGroupPrivileges hndl=DLAKSHDLKJHJjSADHLKJhlkASHLH gid=HJjLHSADHhlkASHDLAKSHDLKJLKJhlk -"
