get:
  operationId: get_group_spaces_in_effective_provider
  tags:
    - Group
  x-onedata-gri:
    type: od_provider
    id: ?BINDING(pid)
    aspect: "{group_spaces, ?BINDING(id)}"
    scope: private
  x-onedata-permissions:
    - group_view
  summary: Get group's spaces that are supported by given effective provider
  x-bash-codegen-description: |
    Returns the list of group's spaces that are supported by given effective provider.

    This operation requires `group_view` privilege.
  description: |
    Returns the list of group's spaces that are supported by given effective provider.

    This operation requires `group_view` privilege.

    ***Example cURL requests***

    **Get groups's spaces supported by effective provider**
    ```bash
    curl -u username:password -X GET \
    https://$HOST/api/v3/onezone/groups/HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8/effective_providers/HUUPOMNGzikwiLXHaFYcE8-qxDtKmt1Gb3v5OnF9UNE/spaces

    {
      "spaces": [
        "ASmlkZW50aWZpZXIgOEhmSEFSSGdrbHFCa1pWSTR"
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Returns the list of group's spaces that are supported by given effective provider.
      schema:
        $ref: '#/definitions/ProviderDetails'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Authentication error.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Authorization error.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Resource not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server Error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    in: path
    description: Group ID.
    type: string
    required: true
  - name: pid
    in: path
    description: Effective provider ID.
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: "onezone-rest-cli getGroupSpacesInEffectiveProvider id=HwUpk8jrwxKOe45uzLFX2GVC8lKEasj4q253sptVqF8 pid=HUUPOMNGzikwiLXHaFYcE8"
